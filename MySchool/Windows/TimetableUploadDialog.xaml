<Window x:Class="MySchool.Windows.TimetableUploadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Upload Timetable"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush.Background}"
        Width="700"
        Height="600"
        WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource Font.Main}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                             ResizeBorderThickness="0"
                             CornerRadius="16"
                             GlassFrameThickness="0"
                             UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource Brush.Background}" CornerRadius="16" BorderThickness="1" BorderBrush="{DynamicResource Brush.Primary}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Upload Timetable Data" Style="{StaticResource Text.Headline}"/>
                <Button Style="{StaticResource CaptionButton.Close}" 
                        HorizontalAlignment="Right" 
                        Click="CloseButton_Click"
                        ToolTip="Close">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Text="&#xE8BB;"/>
                </Button>
            </Grid>

            <!-- Instructions -->
            <TextBlock Grid.Row="1" Style="{StaticResource Text.Body}" Margin="0,0,0,12" TextWrapping="Wrap">
                <Run Text="Step 1: Copy the prompt below and paste it into ChatGPT, Claude, or any AI assistant."/>
                <LineBreak/>
                <Run Text="Step 2: Follow the AI's instructions to provide your timetable information."/>
                <LineBreak/>
                <Run Text="Step 3: Copy the AI's JSON response and paste it in the box below."/>
            </TextBlock>

            <!-- Prompt Display -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="Prompt for AI Assistant" Style="{StaticResource Text.Title}"/>
                        <Button x:Name="CopyPromptButton" 
                                Style="{StaticResource Button.Secondary}" 
                                Content="Copy Prompt" 
                                HorizontalAlignment="Right"
                                Padding="12,6"
                                Click="CopyPromptButton_Click"/>
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="150">
                        <TextBox x:Name="PromptTextBox" 
                                 IsReadOnly="True" 
                                 TextWrapping="Wrap"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="12"
                                 Foreground="{DynamicResource Brush.TextSecondary}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Label for response input -->
            <TextBlock Grid.Row="3" Text="Paste AI Response (JSON only)" Style="{StaticResource Text.Title}" Margin="0,8,0,8"/>

            <!-- Response Input -->
            <Border Grid.Row="4" Style="{StaticResource Card}" Margin="0,0,0,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="ResponseTextBox" 
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             BorderThickness="0"
                             Background="Transparent"
                             FontFamily="Consolas"
                             FontSize="12"
                             Foreground="{DynamicResource Brush.TextPrimary}"
                             MinHeight="100"/>
                </ScrollViewer>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource Button.Secondary}" 
                        Content="Cancel" 
                        Padding="16,8"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="SaveButton" 
                        Style="{StaticResource Button.Primary}" 
                        Content="Save Timetable" 
                        Padding="16,8"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
