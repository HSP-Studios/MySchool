<Window x:Class="MySchool.Windows.SubjectShortenerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Long Subject Names"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush.Background}"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource Font.Main}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                             ResizeBorderThickness="0"
                             CornerRadius="16"
                             GlassFrameThickness="0"
                             UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource Brush.Background}" CornerRadius="16" BorderThickness="1" BorderBrush="{DynamicResource Brush.Primary}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Long Subject Names" Style="{StaticResource Text.Headline}"/>
                <Button Style="{StaticResource CaptionButton.Close}" 
                        HorizontalAlignment="Right" 
                        Click="CloseButton_Click"
                        ToolTip="Close">
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Text="&#xE8BB;"/>
                </Button>
            </Grid>

            <!-- Subtitle -->
            <TextBlock Grid.Row="1" 
                       Style="{StaticResource Text.Body}" 
                       Margin="0,0,0,16" 
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.TextSecondary}">
                These subjects have long names. Please shorten them to 8 characters or less for better display.
            </TextBlock>

            <!-- Subject List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                <StackPanel x:Name="SubjectsPanel">
                    <!-- Subject items will be added here dynamically -->
                </StackPanel>
            </ScrollViewer>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource Button.Secondary}" 
                        Content="Cancel" 
                        Padding="16,8"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button x:Name="SaveButton" 
                        Style="{StaticResource Button.Primary}" 
                        Content="Save Changes" 
                        Padding="16,8"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
